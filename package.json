{
  "name": "atlas-installed-base",
  "version": "0.1.0",
  "type": "module",
  "license": "MIT",
  "description": "Base tecnica local de ATLAS para inteligencia de equipos instalados",
  "scripts": {
    "setup": "python tools/prepare_environment.py",
    "models:download": "node tools/download-models.js",
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests && python -m pytest -q",
    "format:check": "prettier --check package.json tsconfig.json \".github/**/*.yml\" \"tools/**/*.js\"",
    "check": "npm run lint && npm run typecheck && npm run format:check && python -m ruff check . && npm test"
  },
  "dependencies": {
    "@qvac/sdk": "0.19.0"
  },
  "devDependencies": {
    "@eslint/js": "10.0.1",
    "@types/node": "26.3.0",
    "eslint": "10.9.1",
    "globals": "17.11.0",
    "prettier": "3.9.6",
    "tsx": "4.23.12",
    "typescript": "6.0.3",
    "typescript-eslint": "8.68.0",
    "vitest": "4.1.11"
  }
}
